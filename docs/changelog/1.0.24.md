# Vesper Launcher 1.0.24

- Fixed launcher-to-launcher skin sync in multiplayer by moving the shared skin registry to the Vesper Cloudflare API.
- Existing installs now refresh `skin-sync.json`, so outdated clients stop using the dead jsonblob registry after update.
