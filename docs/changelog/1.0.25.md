# Vesper Launcher 1.0.25

- Fixed auto-update logging the player out after launcher restart. The launcher now keeps the cloud session between updates without writing local password hashes back to disk.
- Fixed silent updates recreating the desktop shortcut if the player had already removed it.
