### Что обновили

- автообновление больше не зависит от забитого `C:` и по умолчанию качает пакет в локальную папку `.launcher-updates` рядом с лаунчером
- если настроенная временная папка переполнена, лаунчер сам переключается на запасной путь на диске установки
- исправлен повторный срыв после неудачной загрузки обновления
